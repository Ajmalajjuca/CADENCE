type Brief = { topic: string; angle: string; facts: Array<{ text: string; sourceUrl: string }>; sources: Array<{ url: string; title: string; note: string }> };
export function ResearchPanel({ brief }: { brief: Brief }) {
  return <section className="mt-6 rounded-xl border p-5"><h2 className="text-xl font-semibold">Research</h2><p className="mt-2 text-slate-600">{brief.angle}</p>{brief.facts.length ? <ul className="mt-4 space-y-2">{brief.facts.map((fact,index) => <li key={index}>{fact.text} <a href={fact.sourceUrl} target="_blank" rel="noopener noreferrer" className="text-sm underline">Source</a></li>)}</ul> : <p className="mt-4 text-sm text-slate-600">No verified factual claims found. This post will be framed as an opinion.</p>}{brief.sources.length > 0 && <div className="mt-4"><h3 className="font-medium">Sources</h3><ul className="mt-2 list-inside list-disc">{brief.sources.map(source => <li key={source.url}><a href={source.url} target="_blank" rel="noopener noreferrer" className="underline">{source.title || source.url}</a></li>)}</ul></div>}</section>;
}
