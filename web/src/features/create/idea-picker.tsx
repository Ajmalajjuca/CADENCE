type Idea = { id: string; title: string; angle: string; whyNow: string; pillar: string; sourceUrls: string[] };
export function IdeaPicker({ ideas, onChoose, busy }: { ideas: Idea[]; onChoose: (id: string) => void; busy: boolean }) {
  return <section aria-labelledby="ideas-heading" className="mt-6"><h2 id="ideas-heading" className="text-xl font-semibold">Choose an idea</h2><div className="mt-4 space-y-3">{ideas.map(idea => <article key={idea.id} className="rounded-xl border p-5"><p className="text-xs uppercase tracking-wide text-slate-500">{idea.pillar}</p><h3 className="mt-2 font-semibold">{idea.title}</h3><p className="mt-2 text-slate-600">{idea.angle}</p>{idea.whyNow && <p className="mt-2 text-sm text-slate-500">Why now: {idea.whyNow}</p>}{idea.sourceUrls.length > 0 && <div className="mt-2 flex flex-wrap gap-2">{idea.sourceUrls.map(url => <a key={url} href={url} target="_blank" rel="noopener noreferrer" className="text-sm underline">Source</a>)}</div>}<button disabled={busy} onClick={() => onChoose(idea.id)} className="mt-4 rounded-lg bg-slate-900 px-4 py-2 text-white disabled:opacity-50">Choose this idea</button></article>)}</div></section>;
}
